'use strict';

/**
 * locations-we-serve-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::locations-we-serve-page.locations-we-serve-page');
