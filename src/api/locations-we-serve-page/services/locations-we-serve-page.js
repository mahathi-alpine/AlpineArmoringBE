'use strict';

/**
 * locations-we-serve-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::locations-we-serve-page.locations-we-serve-page');
