'use strict';

/**
 * inventory-vehicle controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::inventory-vehicle.inventory-vehicle');
