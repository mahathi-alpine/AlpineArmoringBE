'use strict';

/**
 * vehicles-alternative service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vehicles-alternative.vehicles-alternative');
