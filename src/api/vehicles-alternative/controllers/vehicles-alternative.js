'use strict';

/**
 * vehicles-alternative controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vehicles-alternative.vehicles-alternative');
