'use strict';

/**
 * vehicles-alternative router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vehicles-alternative.vehicles-alternative');
