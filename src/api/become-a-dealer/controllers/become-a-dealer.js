'use strict';

/**
 * become-a-dealer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::become-a-dealer.become-a-dealer');
