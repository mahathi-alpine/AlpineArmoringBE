'use strict';

/**
 * swat-listing-model service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::swat-listing-model.swat-listing-model');
