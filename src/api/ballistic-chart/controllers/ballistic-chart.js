'use strict';

/**
 * ballistic-chart controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ballistic-chart.ballistic-chart');
